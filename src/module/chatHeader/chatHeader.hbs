<section class="header">
    <div class="header__left">

        {{{ button }}}

        {{#if withInput}}
            {{{ input }}}
        {{/if}}

        {{#if withHeaderTitle}}
            <h1 class="header__title">{{ headerTitle }}</h1>
        {{/if}}

    </div>
</section>