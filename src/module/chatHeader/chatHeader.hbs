<section class="header">
    <div class="header__left">

        {{>button buttonContex}}

        {{#if withInput}}
            {{>input inputContex}}
        {{/if}}

        {{#if withHeaderTitle}}
            <h1 class="header__title">{{ headerTitle }}</h1>
        {{/if}}

    </div>
</section>